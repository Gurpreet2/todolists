
<% include ../partials/navheader %>

<script type="text/javascript" src="/assets/js/lists/list-animator.js" defer async></script>

<h2><%= list.name %></h2>

<h6><%= list.description %></h6>

<form action="/lists/<%= list._id %>/items" method="POST">
  <input type="text" name="item[text]" placeholder="Add a new item">
  <input type="hidden" name="item[completed]" value="false">
  <button class="btn btn-default">Submit</button>
</form>

<div id="abcdef">hi there</div>

<ul id="<%= list._id %>" class="list-list">
  <% list.items.forEach(function(item) { %>
    <li id="<%= item._id %>" class="list-item">
      <span class="list-item-text"><%= item.text %></span>
      <span class="list-item-edit-button list-item-button">Edit</span>
      <form class="list-item-button" action="/lists/<%= list._id %>/items/<%= item._id %>?_method=DELETE" method="POST"><button class="btn btn-sm btn-danger"><i class="fas fa-trash-alt"></i></button></form>
    </li>
  <% }); %>
</ul>

<a class="btn btn-default" href="/lists/<%= list._id %>/edit">Edit</a>
<form action="/lists/<%= list._id %>?_method=DELETE" method="POST">
  <button class="btn btn-warning" type="Submit">Delete List</button>
</form>

<% include ../partials/footer %>
